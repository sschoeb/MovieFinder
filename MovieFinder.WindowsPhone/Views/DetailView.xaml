<views:MvxPhonePage
    x:Class="MovieFinder.WindowsPhone.Views.DetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:Cirrious.MvvmCross.WindowsPhone.Views;assembly=Cirrious.MvvmCross.WindowsPhone"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <StackPanel x:Name="LayoutRoot" Background="Transparent" Margin="12,0,12,0">
        
        <TextBlock Text="Title:" FontWeight="Bold"/>
        <TextBlock Text="{Binding Movie.title}"/>
        <TextBlock Text="Year:" FontWeight="Bold" Margin="0,8,0,0"/>
        <TextBlock Text="{Binding Movie.year}"/>
        <TextBlock Text="Synopsis:" FontWeight="Bold" Margin="0,8,0,0"/>
        <TextBlock Text="{Binding Movie.synopsis}"/>
        
    </StackPanel>

</views:MvxPhonePage>